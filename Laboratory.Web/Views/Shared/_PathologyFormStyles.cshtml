<style>
	/* Pathology-style compact form (shared) */
	.pathology-card {
		border: 1px solid #ccc;
		padding: 12px;
		border-radius: 6px;
		background: #fff;
		font-family: "Segoe UI", Arial, sans-serif;
		font-size: 13px;
		color: #222;
	}

	.pathology-header {
		display: flex;
		justify-content: space-between;
		align-items: center;
		margin-bottom: 10px;
	}

	.pathology-header h2 {
		margin: 0;
		font-size: 15px;
		letter-spacing: 0.5px;
	}

	.form-grid {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 10px 16px;
	}

	.field-row {
		display: flex;
		align-items: center;
		gap: 10px;
		padding: 4px 0;
	}

	.field-label {
		width: 140px;
		font-weight: 600;
		color: #333;
		white-space: nowrap;
	}

	.field-input {
		flex: 1;
	}

	.compact-input, .compact-select {
		width: 100%;
		padding: 6px 8px;
		border: 1px solid #cfcfcf;
		border-radius: 3px;
		font-size: 13px;
	}

	.test-type {
		margin-top: 10px;
		padding: 8px;
		border: 1px solid #bbbbbb;
		border-radius: 4px;
		background: linear-gradient(#fff,#fafafa);
	}

	.submit-row {
		text-align: center;
		margin-top: 10px;
	}

	/* Compact table styles for dense rows */
	.table-compact th,
	.table-compact td {
		padding: 6px 8px !important;
		vertical-align: middle;
		font-size: 13px;
	}

	.table-compact tbody tr { height: 30px; }

	/* Small helper for dense labels */
	.small-label { font-size: 12px; }

	@@media (max-width: 768px) {
		.form-grid {
			grid-template-columns: 1fr;
		}
		.field-label { width: 120px; }
	}
</style>
